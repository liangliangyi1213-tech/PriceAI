"use client";
import { useState, type FormEvent } from "react";
import { useRouter } from "next/navigation";
import { normalizeSearchQuery } from "@/lib/search-query";
const hot = ["iPhone", "华为", "小米", "OPPO", "vivo"];
export function SearchForm() { const router=useRouter(); const [query,setQuery]=useState(""); const go=(value:string)=>{const q=normalizeSearchQuery(value);if(q)router.push(`/search?q=${encodeURIComponent(q)}`);}; function submit(e:FormEvent){e.preventDefault();go(query);} return <div className="mx-auto mt-9 w-full max-w-3xl"><form className="rounded-2xl bg-white p-2 shadow-xl shadow-blue-950/10 ring-1 ring-slate-200 sm:flex" onSubmit={submit}><label className="sr-only" htmlFor="phone-search">搜索手机型号</label><input className="h-12 w-full rounded-xl px-4 text-base outline-none placeholder:text-slate-400 focus:ring-2 focus:ring-blue-600 sm:h-14" id="phone-search" type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="搜索手机，例如：iPhone 16 Pro、华为 Mate 70……"/><button className="mt-2 h-12 w-full rounded-xl bg-blue-600 px-6 font-semibold text-white hover:bg-blue-700 sm:mt-0 sm:h-14 sm:w-auto" type="submit">开始搜索</button></form><div className="mt-5 flex flex-wrap justify-center gap-2"><span className="self-center text-sm text-slate-500">热门搜索</span>{hot.map(t=><button className="min-h-10 rounded-full border border-blue-100 bg-white px-4 text-sm font-medium text-slate-700 hover:bg-blue-50" key={t} onClick={()=>go(t)} type="button">{t}</button>)}</div></div>; }
