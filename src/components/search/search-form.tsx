"use client";
import { useState, type FormEvent } from "react";
import { useRouter } from "next/navigation";
import { normalizeSearchQuery } from "@/lib/search-query";
const hot = ["iPhone 16 Pro", "小米 15", "华为 Mate 70 Pro", "vivo X200"];
export function SearchForm({ align = "center" }: { align?: "center" | "start" }) { const router=useRouter(); const [query,setQuery]=useState(""); const go=(value:string)=>{const normalized=normalizeSearchQuery(value);if(normalized)router.push(`/search?q=${encodeURIComponent(normalized)}`);}; function submit(event:FormEvent){event.preventDefault();go(query);} return <div className={`mt-7 w-full max-w-3xl text-left sm:mt-8 ${align==="center"?"mx-auto":""}`}><form className="rounded-2xl border border-slate-200 bg-white p-2 shadow-[0_16px_45px_-20px_rgba(37,99,235,0.35)] sm:flex sm:items-center" onSubmit={submit}><label className="sr-only" htmlFor="phone-search">搜索商品</label><div className="flex min-w-0 flex-1 items-center gap-3 px-3"><svg aria-hidden="true" className="size-5 shrink-0 text-slate-400" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24"><circle cx="11" cy="11" r="6"/><path d="m20 20-4.2-4.2"/></svg><input className="h-11 min-w-0 flex-1 bg-transparent text-base text-slate-950 outline-none placeholder:text-slate-400 sm:h-12" id="phone-search" type="search" value={query} onChange={(event)=>setQuery(event.target.value)} placeholder="搜索你想买的商品，例如 iPhone 16 Pro"/></div><button className="mt-2 h-11 w-full whitespace-nowrap rounded-xl bg-blue-600 px-6 text-sm font-semibold text-white transition-colors hover:bg-blue-700 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600 sm:mt-0 sm:h-12 sm:w-auto" type="submit">开始比价</button></form><div className={`mt-4 flex flex-wrap items-center gap-2 ${align==="center"?"justify-center text-center":"justify-start"}`}><span className="mr-1 text-sm text-slate-500">热门搜索</span>{hot.map((item)=><button className="min-h-9 shrink-0 whitespace-nowrap rounded-full border border-slate-200 bg-white px-3 text-sm font-medium text-slate-700 transition-colors hover:border-blue-200 hover:bg-blue-50 hover:text-blue-700" key={item} onClick={()=>go(item)} type="button">{item}</button>)}</div></div>; }
