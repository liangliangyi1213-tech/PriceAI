import type{Product}from"@/types/catalog";import{getLowestOffer}from"@/lib/pricing/offers";export type SortMode="recommended"|"price"|"rating"|"sales";export const sortProducts=(rows:{product:Product;score:number}[],m:SortMode)=>[...rows].sort((a,b)=>{const x=getLowestOffer(a.product.variants[0].offers)!;const y=getLowestOffer(b.product.variants[0].offers)!;return m==="price"?x.price-y.price:m==="rating"?y.rating-x.rating:m==="sales"?y.sales-x.sales:b.score-a.score});
