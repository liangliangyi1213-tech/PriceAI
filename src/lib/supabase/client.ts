import{createClient}from"@supabase/supabase-js"; const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY; export const isSupabaseConfigured=Boolean(url&&key); export const getSupabase=()=>{if(!url||!key)throw new Error("Supabase 未配置：请设置 NEXT_PUBLIC_SUPABASE_URL 和 NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY。");return createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}})};
