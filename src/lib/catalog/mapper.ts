import type{Product}from"@/types/catalog";import type{CatalogRow,VariantRow,OfferRow}from"@/lib/supabase/database.types";export const mapCatalog=(products:CatalogRow[],variants:VariantRow[],offers:OfferRow[]):Product[]=>products.map(p=>({...p,variants:variants.filter(v=>v.product_id===p.id).map(v=>({id:v.id,productId:v.product_id,storage:v.storage,color:v.color,region:v.region,condition:v.condition,performance:v.performance,offers:offers.filter(o=>o.variant_id===v.id).map(o=>({id:o.id,variantId:o.variant_id,platform:o.platform,seller:o.seller,title:o.title,price:Number(o.price),originalPrice:o.original_price===null?undefined:Number(o.original_price),rating:Number(o.rating),sales:o.sales,shipping:o.shipping,warranty:o.warranty,url:o.url,updatedAt:o.updated_at,matchConfidence:Number(o.match_confidence)}))}))}));
